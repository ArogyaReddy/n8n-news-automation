# Railway will automatically set PORT variable
# We need to configure n8n to use it

# Basic n8n config
N8N_HOST=0.0.0.0
N8N_PROTOCOL=http
N8N_LOG_LEVEL=info
GENERIC_TIMEZONE=UTC

# Disable auth for now (can enable later)
N8N_BASIC_AUTH_ACTIVE=false

# Use SQLite (no external DB needed)
DB_TYPE=sqlite

# Simple encryption key
N8N_ENCRYPTION_KEY=simple-encryption-key-for-railway